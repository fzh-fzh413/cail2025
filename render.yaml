services:
  - type: web
    name: legal-ai-api
    env: python
    buildCommand: pip install -r requirements.txt && pip install gunicorn
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 8000
      # 在 Render 控制台设置 DASHSCOPE_API_KEY，不要写在这里




